<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yupi.aicodehelper.mapper.UserMapper">
    
    <resultMap id="userMap" type="com.yupi.aicodehelper.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    
    <!-- 根据用户名查询用户信息 -->
    <select id="findByUsername" resultMap="userMap">
        SELECT * FROM user WHERE username = #{username}
    </select>
    
    <!-- 查询用户的角色列表 -->
    <select id="findRolesByUserId" resultType="java.lang.String">
        SELECT r.role_name FROM role r
        JOIN user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>
    
    <!-- 查询用户的权限列表 -->
    <select id="findPermissionsByUserId" resultType="java.lang.String">
        SELECT p.perm_name FROM permission p
        JOIN role_permission rp ON p.id = rp.perm_id
        JOIN user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>
    
    <!-- 插入新用户 -->
    <insert id="insert" parameterType="com.yupi.aicodehelper.entity.User">
        INSERT INTO user (username, password, nickname, email, phone, status)
        VALUES (#{username}, #{password}, #{nickname}, #{email}, #{phone}, #{status})
    </insert>
    
    <!-- 更新用户信息 -->
    <update id="update" parameterType="com.yupi.aicodehelper.entity.User">
        UPDATE user
        SET nickname = #{nickname},
            email = #{email},
            phone = #{phone},
            status = #{status}
        WHERE id = #{id}
    </update>
</mapper>